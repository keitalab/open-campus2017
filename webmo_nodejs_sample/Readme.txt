このままでは動かない。
cmdやbashなどでこのフォルダーを開き以下のコマンドを実行する。

npm install webmo-client-nodejs --save

すると「node-modules」というフォルダーができ、index.jsを実行することが可能になる。
実行コマンドは「node index.js」(省略して「node index」、「node .」でも実行可能)

